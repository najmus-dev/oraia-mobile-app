{
  "name": "oraia-mobile-app",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w oraia-api",
    "dev:mobile": "npm start -w oraia-mobile",
    "test": "npm test -ws --if-present",
    "test:api": "npm test -w oraia-api",
    "test:mobile": "npm test -w oraia-mobile",
    "build:api": "npm run build -w oraia-api"
  }
}
