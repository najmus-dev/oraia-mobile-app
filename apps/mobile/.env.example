# Baked into release builds via EAS (see eas.json). Dev/Expo Go ignores this unless set locally.
EXPO_PUBLIC_API_URL=https://oraia-mobile-app.onrender.com

# Optional override (default is set in app.config.ts after eas init)
# EAS_PROJECT_ID=33e719d6-bce8-416e-9836-97d7bc6311a3
